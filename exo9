#include <stdio.h>
#include <math.h>
int main()
{
  /* algorithme:nombre pair entre 1 et N
    VARIABLE:
      N:entier
      i:entier
    DEBUT
      AFFICHER "Entrer le nombre"
      LIRE (N)
      POUR (i=0;i<=N)
        SI (imod2 =0) ALORS
          AFFICHER (i)
      FINSI
    FINPOUR
  FIN
  */
  int N,i;
  printf ("Entrer le nombre\n");
  scanf ("%d",&N);
  for (i=0;i<=N;i++)
    {
      if (i%2==0)
        {
          printf ("%d\n",i);
        }
    }
  return (0);
}
